// import Image from "next/image";

const MeetUp = ({ id }) => {
  return (
    <section className="w-full flex flex-col justify-center items-start p-15">
      {/* <Image  /> */}
    </section>
  );
};

export default MeetUp;
