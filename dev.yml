version: "3.8"

services:
  server:
    image: app
    deploy:
      replicas: 1
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "200m"
        max-file: "10"
    ports:
      - 3000:3000
    command: --addr :3000
